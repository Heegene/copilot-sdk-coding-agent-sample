name: 📝 Copilot Docs
on:
  issues:
    types: [labeled]
  pull_request:
    types: [labeled]

jobs:
  docs:
    if: |
      github.event.label.name == 'copilot-docs'
    uses: ./.github/workflows/copilot-docs-master.yml
    secrets: inherit
